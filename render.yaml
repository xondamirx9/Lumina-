services:
  - type: web
    name: lumina-voyages
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 10000
    disk:
      name: lumina-db
      mountPath: /data
      sizeGB: 1
